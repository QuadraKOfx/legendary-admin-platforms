

const initialState = {
    document: null,
    isPending: false,
    error: null,
    success: null
}

const clientReducer = (state = initialState, actions) => {
    switch (actions.type) {
        default:
            return state;
    }
}


